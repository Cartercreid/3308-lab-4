#include<stdlib.h>
#include<stdio.h>

#define TRUE 1
#define FALSE 0

int avoid_object();
int going_to_hit_something;

int main()
{
	int car = TRUE;
	int car_on = TRUE;
	while(car == on)
	{
		if (going_to_hit_something == TRUE)
		{
			if (avoid_object())
			{
				sleep(1);
			}
			//update on car crash status
			else
			{
				car_on = FALSE;
				car = FALSE;
			}
		}

	}
	return 0;
} 	

